name: 'shell-check'
on:
  pull_request:

shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: shellcheck
        uses: reviewdog/action-shellcheck@v1
        with:
          pattern: "*.sh"
